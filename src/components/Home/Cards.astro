---
import { getTranslations, type Language } from '../../i18n';

interface Props {
	lang?: Language;
}

const { lang = 'es' } = Astro.props;
const t = getTranslations(lang);
---

<div
    class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid md:grid-cols-2 lg:grid-cols-4 gap-8"
>
    <!-- Feature 1 -->
    <div
        class="p-8 rounded-3xl bg-rose-50 border-b-4 border-[#D9625E] card-shadow transition-transform hover:-translate-y-2"
    >
        <div
            class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center mb-6 shadow-sm"
        >
            <span class="text-2xl">✨</span>
        </div>
        <h3 class="text-xl font-bold mb-3">{t.home.features.card1Title}</h3>
        <p class="text-slate-600">
            {t.home.features.card1Description}
        </p>
    </div>
    <!-- Feature 2 -->
    <div
        class="p-8 rounded-3xl bg-blue-50 border-b-4 border-[#7BA5F2] card-shadow transition-transform hover:-translate-y-2"
    >
        <div
            class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center mb-6 shadow-sm"
        >
            <span class="text-2xl">🤖</span>
        </div>
        <h3 class="text-xl font-bold mb-3">{t.home.features.card2Title}</h3>
        <p class="text-slate-600">
            {t.home.features.card2Description}
        </p>
    </div>
    <!-- Feature 3 -->
    <div
        class="p-8 rounded-3xl bg-rose-50 border-b-4 border-[#D9625E] card-shadow transition-transform hover:-translate-y-2"
    >
        <div
            class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center mb-6 shadow-sm"
        >
            <span class="text-2xl">❤️</span>
        </div>
        <h3 class="text-xl font-bold mb-3">{t.home.features.card3Title}</h3>
        <p class="text-slate-600">
            {t.home.features.card3Description}
        </p>
    </div>
    <!-- Feature 4 -->
    <div
        class="p-8 rounded-3xl bg-blue-50 border-b-4 border-[#7BA5F2] card-shadow transition-transform hover:-translate-y-2"
    >
        <div
            class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center mb-6 shadow-sm"
        >
            <span class="text-2xl">📈</span>
        </div>
        <h3 class="text-xl font-bold mb-3">{t.home.features.card4Title}</h3>
        <p class="text-slate-600">
            {t.home.features.card4Description}
        </p>
    </div>
</div>
